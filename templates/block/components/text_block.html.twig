
<div class="container-fluid p-0">

    {{ form_start(form) }}

            <div class="row justify-content-center">

                <div class="col-12">
                    {{ form_label(form.content, "app.backend.forms.edit_block_contents.labels.content"|trans) }}
                    {{ form_widget(form.content, {
                        "attr": {
                            "class" : "form-control tinymce",
                            "rows" : "10",
                            "placeholder" : "app.backend.forms.edit_block_contents.placeholders.content"|trans
                        }
                    }) }}
                </div>

                <div class="col-12 mt-2">
                    <button type="submit" class="btn btn-block btn-primary">{{ "app.backend.forms.edit_block_contents.buttons.submit"|trans }}</button>
                </div>

            </div>

    {{ form_end(form) }}

</div>